<!DOCTYPE html>
<html lang="ro">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>QR Code</title>
    <style>
      body {
        font-family: sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        margin: 0;
        background: #f5f5f5;
      }
      h1 {
        margin-bottom: 1.5rem;
        color: #333;
      }
      img {
        width: 400px;
        height: 400px;
        border: 8px solid white;
        border-radius: 8px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.15);
      }
      a {
        margin-top: 1.5rem;
        color: #0070f3;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <h1>QR Code</h1>
    <img src="<%= qrDataUrl %>" alt="QR Code" />
    <h6 style="text-align: center">
      Sau adauga manual link-ul <br /><%= qrDataUrlOnly %>
    </h6>

    <a href="/">← Înapoi</a>
  </body>
</html>
